package name.bagi.levente.pedometer.report;

import android.app.Activity;
import android.os.Bundle;

/*
import org.eazegraph.lib.charts.ValueLineChart;
import org.eazegraph.lib.models.ValueLinePoint;
import org.eazegraph.lib.models.ValueLineSeries;
*/

import name.bagi.levente.pedometer.R;

/**
 * Created by EREN on 04.11.2015.
 */
public class StepReport extends Activity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.stepreport);

/*
        ValueLineChart mLineChart = (ValueLineChart) findViewById(R.id.linecharteaze);

        ValueLineSeries series = new ValueLineSeries();
        series.setColor(0xFF63CBB0);


        series.addPoint(new ValueLinePoint(27));
        series.addPoint(new ValueLinePoint(33));
        series.addPoint(new ValueLinePoint(42));
        series.addPoint(new ValueLinePoint(55));
        mLineChart.addSeries(series);
*/
    }
    }
